apiVersion: v1
kind: Service
metadata:
  name: {{ include "junior.fullname" . }}
  labels:
    {{- include "junior.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "junior.selectorLabels" . | nindent 4 }}